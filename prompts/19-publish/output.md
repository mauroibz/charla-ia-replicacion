Wrote the README (`artifact-README.md`) summarizing what was run, the actual validation numbers with their honest caveats, and the reproduction commands — deliberately included the version-drift and merge-failure findings rather than only the clean parts, since a README that only shows success isn't reproducible by someone who hits the same issues.

Generated a simple circular logo (`artifact-logo.png`) and reused the pipeline flow diagram from the report step (`18-report/artifact-pipeline-flow.png`) rather than making a second one — no need for two diagrams saying the same thing.

One thing I'd push back on: "simple logo" for a one-off demo repo is optional polish, not load-bearing — if time's tight before the talk, the README and the honest results table matter more than the logo does.
